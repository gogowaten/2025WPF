<Window x:Class="_20250412.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20250412"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Canvas x:Name="MyCanvas">
      <Grid>
        <Grid.RenderTransform>
          <TransformGroup>
          <ScaleTransform ScaleX="{Binding ElementName=sx, Path=Value}" ScaleY="{Binding ElementName=sy, Path=Value}"/>
            <RotateTransform Angle="{Binding ElementName=angle, Path=Value}"/>
          </TransformGroup>
        </Grid.RenderTransform>

        <local:GeoShapeWithAnchorHandle x:Name="MyShape"
          MyPoints="0,0,100,100" Stroke="Red" StrokeThickness="50"/>
      </Grid>
    </Canvas>

    <StackPanel Grid.Column="1">
      <Button Content="test" Click="Button_Click"/>
      <TextBlock Text="{Binding ElementName=MyShape, Path=MyPoints}"/>
      <Slider x:Name="angle" Minimum="0" Maximum="360"/>
      <Slider x:Name="sx" Minimum="1" Maximum="3" Value="1"/>
      <Slider x:Name="sy" Minimum="1" Maximum="3" Value="1"/>
    </StackPanel>
  </Grid>
</Window>
