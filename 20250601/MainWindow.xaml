<Window x:Class="_20250601.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20250601"
        mc:Ignorable="d"
        Title="MainWindow" Height="367" Width="654">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Canvas x:Name="MyCanvas">
      <Canvas.ContextMenu>
        <ContextMenu>
          <MenuItem Header="canvas"/>
        </ContextMenu>
      </Canvas.ContextMenu>
      <Rectangle Width="100" Height="100" Fill="Tomato">
        <Rectangle.ContextMenu>
          <ContextMenu>
            <MenuItem Header="aaa"/>
          </ContextMenu>
        </Rectangle.ContextMenu>
      </Rectangle>
      <Rectangle Width="50" Height="50" Fill="DodgerBlue" Canvas.Left="200" Canvas.Top="40"/>
    </Canvas>

    <StackPanel Grid.Column="1">

    </StackPanel>
  </Grid>
</Window>
