<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:_20250120_OffsetPolyline">

  
  <Style TargetType="{x:Type local:OffsetPolyline}">
    
    <Setter Property="Template">      
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:OffsetPolyline}">
          <Canvas Width="{Binding MyPolylineDescendantBounds}">
            <Polyline x:Name="PART_polyline"
                    Stroke="DodgerBlue"
                    StrokeThickness="100"
                    Points="10,10,200,100"/>
          </Canvas>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
